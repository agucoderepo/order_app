BACKEND_API_URL=http://localhost:8000

# Credentials for a user with the 'service' role (see backend/app/permissions.py).
# Create this user through the admin Users page with role=service.
MCP_SERVICE_EMAIL=service@example.com
MCP_SERVICE_PASSWORD=change-me

MCP_HOST=0.0.0.0
MCP_PORT=8100
